BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"
BBFILE_COLLECTIONS += "example"
# The guest is Intel TDX; the firmware target below has to match.
DISTRO_FEATURES += "tdx"
